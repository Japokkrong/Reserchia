# Regenerated by the pipeline, and large: ~106 MB of Chroma index and ~76 MB of
# cached vectors. `03_embed.py` rebuilds both, and re-embedding costs ~$0.013.
data/chroma/
data/embedding-cache/

# ~7 MB of chunk dumps across 72 configurations; `02_chunk_eda.py` rebuilds them.
data/chunks/

# Committed on purpose: the source PDFs, the extracted text, the test set, the
# retrieval results, the figures and REPORT.md -- everything needed to read or
# re-check the findings without re-running anything.
